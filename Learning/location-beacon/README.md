This program simulate a BLE peripheral with one custom service and two characteristics - one contains a lat/long and the other contains a URL with beacon identifier.

To run:

sudo service bluetooth stop

sudo npm start
