//- Layout for home(index) page
doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    title Physical Web Sample
    link(rel='stylesheet', href='dijit/themes/tundra/tundra.css')
    link(rel='stylesheet', href='stylesheets/style.css')
    script(src='dojo/dojo.js')
  body.tundra
    div(data-dojo-type='dijit/layout/BorderContainer', style='width: 100%; height: 100%')
      div(data-dojo-type='dijit/layout/ContentPane', data-dojo-props="region:'top'")
        span(style='float:right')
          |#{user.username}
          |(
          a(href="/logout") logout
          |)
      div(data-dojo-type='dijit/layout/ContentPane', data-dojo-props="region:'center'", id="centerContainer")
        | Press the button below to read location from BLE and loads map.
        block content
      div(data-dojo-type='dijit/layout/ContentPane', data-dojo-props="region:'bottom'") 
        button(data-dojo-type='dijit/form/Button', type='button' onClick="onButtonClick();") Read from BLE
    script.
      require(["dojo/parser", "dijit/layout/BorderContainer", "dijit/layout/ContentPane", "dijit/form/Button"], function(parser) {
        parser.parse();
      });